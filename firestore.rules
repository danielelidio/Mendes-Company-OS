rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Authenticated users can read/write. Tighten per-collection as needed.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
